# でんしゃしょうぎ — ゲーム仕様書

## 1. ゲーム概要

未就学児（4〜6歳）が、ひらがなと電車の絵だけで遊べる、ごく簡単な「将棋風」ボードゲーム。
スマートフォン／タブレットのブラウザで、2人が同じ画面を交互にタップして遊ぶ「ホットシート対戦」を基本とする。

- ゲーム名: **でんしゃしょうぎ**
- プレイ人数: 2人（同じ端末で交互に操作）
- 想定プレイ時間: 1局 3〜5分
- 想定操作: タッチ操作のみ（マウスでも動作）
- 文字: すべて**ひらがな**または絵で表示。漢字は使わない。

## 2. 設計の大原則（最重要）

未就学児が「自分でできた！」を体験できることが最優先。これは将棋の正確な再現よりも常に優先する。

1. **読めなくても遊べる**こと。駒・ボタン・状態はすべて絵と色で識別可能にする。
2. **タップ領域は大きく**（最低 60×60px、推奨 72px 以上）。誤タップを許容する。
3. **動ける場所は必ず光って見せる**。駒をタップしたら、行けるマスを緑のまる🟢 で表示する。
4. **間違えた操作は「無音で無視」**。ブザー音や赤いバツは出さない。子どもが萎縮しない。
5. **負けても楽しい演出**。負けた側にも「がんばったね！」と笑顔の電車を出す。
6. **文字を読めない大人（保護者）にも一目で分かる**こと。色とアイコンで状況を伝える。

## 3. 盤・駒・初期配置

### 3.1 盤
- **6行×7列**（縦6×横7）の長方形。
- マスは線路の枕木のような淡い茶色＋白のチェッカー柄。
- 行は手前から「1」、奥に向かって「6」。列は左から1〜7。座標表示は内部のみ（画面には出さない）。
- 駒は中央5列（列2〜6）に配置され、左右端の列（列1・列7）は初期状態では空。

### 3.2 駒の種類（4種類）

| 名前（表示） | 記号 | 役割 | 動き | 数（片側） |
|---|---|---|---|---|
| **ふつうでんしゃ** ◯ | 歩に相当 | 前に1マスだけ | 前1 | 2 |
| **とっきゅうれっしゃ** △ | 飛車に似た存在 | 前後左右に1マス | 十字1 | 1 |
| **かもつれっしゃ** □ | 角に似た存在 | 斜め4方向に1マス | 斜1 | 1 |
| **しんかんせん** ☆ | 王将と同じ | 全方向に1マス | 全8方向1 | 1 |

- 「持ち駒（再使用）」はなし。取った駒は画面の「しゃこ（車庫）」に並べるだけで使えない。
- 同じ色の駒があるマスには動けない。相手の駒があるマスに動くと、その駒を取って車庫に入れる。

### 3.3 成り

相手陣の**最後の2行**に駒が入ると、自動で「成り」になる（強制）。成った駒は金色の背景＋★バッジで表示され、専用の成りアイコンがある駒種はアイコン自体も切り替わる。

- あかの成りゾーン: 行5・行6（あお陣）
- あおの成りゾーン: 行1・行2（あか陣）

| 駒 | 成り後の名前 | 成り後の動き | 成り後のアイコン |
|---|---|---|---|
| ふつうでんしゃ ◯ | **かいそく** | 前後左右に1マス（とっきゅうと同じ） | 専用画像に切り替え |
| とっきゅうれっしゃ △ | **スーパーとっきゅう** | 前1＋斜め4方向（将棋の銀将と同じ） | 専用画像に切り替え |
| かもつれっしゃ □ | **スーパーかもつ** | 前後左右＋前斜め（将棋の金将と同じ） | 通常アイコンのまま（★バッジのみ） |
| しんかんせん ☆ | **スーパーしんかんせん** | 全方位に最大2マス（途中の駒は飛び越えられない） | 専用画像に切り替え |

- 成り後の画像は `assets/images/{red|blue}-{駒種}-promoted.png`。定義が無い駒種（かもつ）は通常画像＋★バッジで表示する（`pieces.js` の `pieceImage()` が自動でフォールバック）。
- 車庫（取られた駒）の表示は成り状態に関わらず通常アイコン。
- すでに成っている駒が成りゾーンに入っても変化しない。
- 「もどす」で成りを取り消すことができる（アイコンも元に戻る）。

### 3.4 初期配置

奥が「あお」プレイヤー（青い電車＝JR東日本モチーフ）、手前が「あか」プレイヤー（赤い電車＝JR西日本・東海モチーフ）。

```
    1    2    3    4    5    6    7
6 [  ][□][  ][☆][  ][△][  ]   ← あお（奥）
5 [  ][  ][◯][  ][◯][  ][  ]
4 [  ][  ][  ][  ][  ][  ][  ]
3 [  ][  ][  ][  ][  ][  ][  ]
2 [  ][  ][◯][  ][◯][  ][  ]
1 [  ][△][  ][☆][  ][□][  ]   ← あか（手前）
```

- ふつうでんしゃ（◯）は片側 2 個（2段目/5段目の 列3・列5）。
- とっきゅうれっしゃ（△）は片側 1 個（あか: 1段目列2、あお: 6段目列6 ← 左右鏡像）。
- かもつれっしゃ（□）は片側 1 個（あか: 1段目列6、あお: 6段目列2 ← 左右鏡像）。
- しんかんせん（☆）は片側 1 個（中央列4）。
- 片側の合計: しんかんせん1 + とっきゅう1 + かもつ1 + ふつう2 = 5 個。
- 列1・列7は初期状態では空だが、ゲーム中に駒を動かすことができる。

### 3.5 駒のデザイン

PNG画像（`assets/images/`、ユーザー提供のフリーアイコン）で、実在のJR車両をモチーフにしたデザイン。ファイル名は `{red|blue}-{futsuu|tokkyuu|kamotsu|shinkansen}.png`、成り後は末尾に `-promoted` を付ける。

| 駒 | あお（JR東日本モチーフ） | あか（JR西日本・東海モチーフ） |
|---|---|---|
| しんかんせん ☆ | E5系はやぶさ（緑） | N700系のぞみ（白青） |
| とっきゅう △ | E353系あずさ（白紫） | 287系くろしお（白緑） |
| かもつ □ | EH500金太郎（赤） | EF210桃太郎（青） |
| ふつう ◯ | E233系（銀＋青帯） | 223系（銀＋茶帯） |

## 4. ゲームの流れ

1. タイトル画面で「はじめる」ボタンをタップ。初回起動時はそのままチュートリアル（§5.7）が流れる。常設の「あそびかた」ボタンからもいつでも再生可能。
2. 「あか」から先手。手前のプレイヤーから始める。
3. 自分の番のプレイヤー側の盤面が**ふわっと光って**「あなたのばん」と表示される。
4. 駒をタップ → 行けるマスが緑のまるで光る → 行きたいマスをタップ → 駒が移動。
5. 別の駒に変えたいときは、空いているマスをタップするか、もう一度同じ駒をタップで選択解除。
6. **相手の「しんかんせん」を取った瞬間に勝ち**。勝利演出に入る。
7. 「もういっかい」ボタンで再戦、「やめる」ボタンでタイトルに戻る。

### 4.1 反則・特殊ルール（すべて「ない」）
- 王手・詰みの判定はしない。**しんかんせんを実際に取った時点で勝ち**。
- 二歩・行き所のない駒・千日手などのルールは一切なし。
- 自殺手（自分のしんかんせんを取らせる動き）も禁止しない。

## 5. UI仕様

### 5.1 画面構成（縦持ちスマホ基準・375×667 想定）

```
┌──────────────────────┐
│  あおの ばん         │  ← 現在のターン表示（背景色も変わる）
├──────────────────────┤
│   あおの しゃこ      │  ← 取った駒置き場（小さく）
│   [🚃][🚃]           │
├──────────────────────┤
│                      │
│   ┌───────────┐      │
│   │           │      │  ← 6×7の盤（画面幅にフィット）
│   │   ば ん   │      │
│   │           │      │
│   └───────────┘      │
│                      │
├──────────────────────┤
│   あかの しゃこ      │
│   [🚄]               │
├──────────────────────┤
│ [やめる]   [もどす]  │  ← 操作ボタン
└──────────────────────┘
```

### 5.1.1 画面サイズ別レイアウト（レスポンシブ要件）

iPhone だけでなく、iPad などのタブレット、横持ち、PC ブラウザでも盤と駒が**読みやすいサイズで中央に大きく**配置されること。CSS のメディアクエリと CSS 変数（`--cell-size` など）で実装し、依存ゼロ要件の範囲を守る。

| 画面区分 | 判定の目安 | 盤のサイズ上限 | 1マスの目安 |
|---|---|---|---|
| スマホ縦持ち | 幅 〜600px | 画面幅の 92% | 約 50〜60px |
| **タブレット縦持ち**（iPad 11/Air など） | 幅 600〜900px | **画面の短辺の 70%** | 約 80〜100px |
| 横持ち（縦より横が長い） | aspect-ratio > 1 | 画面の短辺（=高さ）の 80% | 約 80px |
| PC ブラウザ | 幅 900px 以上 | 画面の短辺の 60%（巨大化させない） | 約 90px |

補足：

- **iPad 11 縦持ちで盤が小さく見える問題**は、既存の CSS が「スマホ幅前提の `max-width`」だけで盤サイズを決めているのが原因。タブレット領域では「画面の短辺」を基準にした拡縮を必ず行うこと。
- 横持ちでは、しゃこ（取った駒置き場）を盤の左右に縦に並べてもよい。
- §5.6 に定めるタッチターゲット最小 60×60 CSS px は、どの画面サイズでも維持する。
- アスペクト比が極端な端末（折りたたみスマホなど）ではフィットすればよく、ピクセル単位の精度は求めない。

### 5.2 駒の見た目
- 実車モチーフのPNGアイコン（§3.5 参照）。成り駒は成り後アイコン＋★バッジ。
- 「あお」プレイヤーの駒は青系、「あか」プレイヤーの駒は赤系のカラーリング。
- 向きは色で識別する（駒の上下反転はしない。子どもが混乱するため）。

### 5.3 ハイライト
- 選択中の駒: 黄色いリング🟡
- 動けるマス（空き）: 緑のまる🟢（やや透けた、直径マスの 50%）
- 取れる相手の駒: オレンジのリング🟠
- **相手駒のピーク表示**: 紫のまる🟣（やや透けた、**直径マスの 35%** — 緑より小さくして形でも区別）。詳細は §5.8。
- **きけんマーク**: 駒の右上に黄色背景＋黒縁の「！」アイコン。詳細は §5.8。

### 5.4 ボタン
- 「もどす（1手戻す）」: 子どもがタップミスした時の救済。1局につき各プレイヤー2回まで。
- 「やめる」: タイトル画面に戻る確認ダイアログ。

### 5.5 アニメーション・効果音

すべての音は **Web Audio API で合成**し、音声ファイルは使わない（依存ゼロ要件）。音量はデフォルト 50%、画面上の 🔊 / 🔈 ボタンで一括ミュート可能（設定は `localStorage` に保存）。

| イベント | 視覚 | 音（Web Audio 合成） |
|---|---|---|
| 駒を選択 | 黄色いリングが付く（0.1秒フェードイン） | 短い「ピッ」（1000Hz サイン波 60ms） |
| **駒の移動** | 0.25 秒で線形に滑る | **「ガタゴト」走行音**（後述 §5.5.1） |
| 駒を取る | 取られる駒が0.2秒でしぼんで消える＋星のパーティクル | 柔らかい「ぽふっ」（ノイズ短音 80ms） |
| 成り（成りゾーン到達） | 金色の背景＋★バッジがふわっと出る（0.3秒） | キラーン（鈴音 200ms） |
| 勝利 | 紙吹雪＋「やったー！」吹き出し | **勝利ファンファーレ**（後述 §5.5.2） |
| 敗北側 | 「がんばったね！」吹き出し＋笑顔の電車 | 控えめな「ピロリン」（短い上昇音 300ms） |

#### 5.5.1 ガタゴト走行音（駒の移動時）

- 駒が動く 0.25 秒の間、電車のレール音っぽい**短いリズム音**を鳴らす。
- 実装イメージ：低めの短いノイズパルス（80Hz〜120Hz、デューティ短い矩形波）を 2回「ガタッ・ゴトッ」と鳴らす。1パルス約 50ms、間隔約 70ms。
- 駒の種類で音色を変えてもよい（任意）：
  - ふつうでんしゃ・とっきゅう・かもつ：低めの「ガタゴト」
  - しんかんせん：少し高めの「シュッ」（ホワイトノイズの短いスイープ）
- 連続して駒を動かす場合（「もどす」→再操作）は、前の音を切ってから新しい音を鳴らす（音が重ならない）。
- ミュート時は鳴らさない。

#### 5.5.2 勝利ファンファーレ

- 約 **2〜3 秒のメジャーキー短旋律**。子どもがびっくりしない音量・音色で合成する。
- 構成案：「ドミソド↑」のアルペジオ →「ソド↑（高音）」で締め、最後に「シャラララン」のキラキラ音（高音ノイズの減衰）。
- サイン波 or 三角波で柔らかく。鋸波・矩形波の強いアタックは避ける。
- 紙吹雪アニメーション（2 秒間）と同期して再生。
- 「もういっかい」をタップしたら即座にフェードアウトして停止。

#### 5.5.3 対局中 BGM（電車走行音ループ）

- ゲーム画面表示中、電車が走る環境音（レール走行音）を**ループ再生**する。
- Web Audio API で合成する。3 層構成：
  1. **ガタンゴトン**（ノイズバースト＋打撃音の 4 連パターン、0.7 秒周期でループ）
  2. **走行ノイズ**（ローパスフィルタ付きホワイトノイズ、控えめに敷く）
  3. **メロディ**（ペンタトニック童謡風の三角波フレーズ、8 小節 = 5.6 秒でループ）
- 音量は控えめ（効果音の 30% 程度）にし、効果音の邪魔にならないようにする。
- ゲーム画面に遷移した時にフェードイン（0.5 秒）で再生開始、タイトル画面・勝利画面に遷移した時にフェードアウト（0.5 秒）して停止する。
- §5.5 共通のミュートボタンで一括 OFF にできる。

#### 5.5.4 音に関する共通ルール

- 効果音は短く優しい音のみ。びっくりさせる音は使わない。
- 駒を 1 秒間に複数回動かそうとした場合（タップ連打）でも、音が重なって耳障りにならないよう、同じイベントの音は**鳴らし直しではなく上書き**する。
- iOS Safari の AudioContext は最初のタップで `resume()` を呼ぶ必要があるため、タイトル画面の「はじめる」タップで初期化する。

### 5.6 アクセシビリティ
- 配色は色覚多様性に配慮（赤緑だけで区別させない。形・太枠も併用）。
- 全ての操作可能要素に `aria-label` と十分なコントラスト。
- タッチターゲットは最小 60×60 CSS px。
- ダブルタップによるズーム抑止は行うが、ピンチズームは許可する。

### 5.7 チュートリアル（あそびかた）

未就学児が**読み聞かせなしで遊び始められる**ように、ひらがな＋アニメーションで遊び方を案内するモード。

#### 5.7.1 起動フロー

- 初回起動時、タイトル画面の「はじめる」タップ直後に**自動で1回だけ**チュートリアルが流れる。
- 完了 or スキップ後、`localStorage` に `tutorialSeen=true` を保存し、以降は自動再生しない。
- タイトル画面に常設の「あそびかた」ボタンを置き、いつでも再生できる。
- 「あそびかた」ボタンは虫眼鏡 🔍 や「？」のアイコン＋ひらがな「あそびかた」を併記（色だけに頼らない）。

#### 5.7.2 構成（合計 6 ステップ・全体で 60〜90 秒）

各ステップは画面に**1つの動作だけ**を見せ、矢印やタップ指マークでデモする。読み上げ音声は付けない。

| # | 内容（ひらがな） | 見せ方 |
|---|---|---|
| 1 | 「これは でんしゃしょうぎ だよ」 | 盤と駒の全体像をフェードイン |
| 2 | 「じぶんの でんしゃを タップしてね」 | デモ盤で「あか」の駒に指マーク→タップ→黄色いリング |
| 3 | 「みどりの まるに タップ」 | 動けるマスに緑のまる→指マーク→駒が滑って移動（ガタゴト音） |
| 4 | 「あいての でんしゃが いると とれる」 | 相手駒のあるマスに移動→「ぽふっ」と消えてしゃこへ |
| 5 | 「おくの 2れつで つよくなる（なり）」 | 駒が成りゾーンに入って成り後アイコン＋金色＋★に変化 |
| 6 | 「あいての しんかんせん☆ を とれたら かち！」 | 自分のしんかんせんが相手のしんかんせんを取る→紙吹雪＋ファンファーレ |

#### 5.7.3 操作 UI

- 画面下に**3つの大きなボタン**（最低 72×72 CSS px）:
  - `← もどる`（最初のステップでは無効・グレーアウト）
  - `スキップ`（中央、目立たない色で）
  - `つぎへ →`（最後のステップでは `はじめる！` に変化）
- ステップインジケータは「●●●○○○」のような点6つで現在位置を視覚化。
- 文字が読めなくても進められるよう、「つぎへ」ボタンには右矢印を必ず添える。

#### 5.7.4 アニメーション仕様

- 各ステップのデモは**自動で1〜2回ループ**する。
- 子どもが触っても本対局には影響しないよう、チュートリアルは**独立したサンドボックス状態**で実行する（`game.js` の状態とは別インスタンス、または読み取り専用モード）。
- ステップ遷移は 0.3 秒のクロスフェード以内。

#### 5.7.5 やらないこと

- 文章だけの長い説明画面は作らない（読めない前提）。
- クイズ形式の「やってみよう」は作らない（できなかったら傷つく）。
- ナレーション音声は入れない（端末・環境差で再生されない不安があるため、視覚で完結させる）。

### 5.8 あいてのうごき可視化（ピーク + きけんマーク）

未就学児が「相手のでんしゃが次にどこへ行きそうか」を理解できないと混乱するため、**2 段構えの補助機能**で見せる。情報過多にならないよう、常時表示は最小限に留める。

#### 5.8.1 ピーク（タップで覗き見）

- **自分の番のあいだ**、相手の駒をタップすると、その駒の動けるマスを **紫のまる🟣** で表示する。
- もう一度同じ相手駒をタップする、または別の場所（盤外含む）をタップすると消える。
- 自分の駒選択（黄色いリング🟡＋緑のまる🟢）と、ピーク表示（紫のまる🟣）は**同時に出さない**。どちらかを表示するとき、もう一方は自動でクリアする。
- 紫は緑との色だけの差で区別しない。**紫のまるは直径マスの 35% と緑より小さく**して、形でも見分けられるようにする。
- ピーク中は実際の指し手にはならない（タップしても駒は動かない、しゃこにも入らない）。

#### 5.8.2 きけんマーク（自動・控えめな警告）

- 自分の番が始まる瞬間に、「**もし相手がいま動かしたら取られる位置にいる自分の駒**」に「！」マークを駒の右上に小さく表示する。
- 計算は**次の1手のみ**を見る単純判定（複数手先は読まない）。`pieces.js` の「動ける範囲」関数を相手側にも適用するだけで済む。
- マークのデザイン：黄色背景＋黒縁の「！」アイコン、駒サイズの約 25%。点滅・拡縮アニメーションは付けない（びっくりさせない原則）。
- 自分が駒を動かしたら、新しい局面で**再計算して表示し直す**。
- ピーク表示中はピーク優先で、きけんマークは一時的に半透明にする（情報の重なりを避ける）。

#### 5.8.3 ヒントのオン／オフ

- タイトル画面の設定で「あぶないよ サイン」を ON/OFF できる。デフォルト ON。
- 設定は `localStorage` に `hintsEnabled`（boolean、デフォルト true）で保存。
- OFF にすると、§5.8.2 のきけんマークは表示しない。§5.8.1 のピークは ON/OFF とは独立に常に使える（タップで見せるだけなので情報過多にならない）。

#### 5.8.4 やらないこと

- 「最善手の指し示し」「取られない安全な動き方の提示」はしない。対局の考える楽しさを奪うため。
- 「！」を派手な色・点滅・効果音で強調しない（§2 設計の大原則）。
- 何手先まで読むかをユーザーに見せない（複雑化する）。常に1手先のみ。
- 相手の番のあいだの相手駒ピークは対象外（相手の番は相手の操作中）。

## 6. ターン管理と勝敗判定

- 内部状態は単純な FSM:
  - `TITLE` → `RED_TURN` ⇄ `BLUE_TURN` → `WIN`
- 駒の移動完了後、相手のしんかんせんを取っていたら `WIN` に遷移。
- 引き分けはなし（時間切れもなし）。

## 7. 非機能要件

- **オフライン動作**: ネットワーク不要。`index.html` をダブルクリックすれば遊べる。
- **依存ゼロ**: ビルドツール・npm install・サーバ不要。
- **ブラウザ対応**: iOS Safari 16+、Android Chrome 最新、PC Chrome / Edge / Safari 最新。
- **端末対応**: iPhone（375〜430px）、Android スマホ、**iPad 11 / iPad Air などのタブレット**（820〜1180px、縦持ち・横持ち両方）、PC ブラウザ（〜1920px）。詳細レイアウトは §5.1.1 参照。
- **保存**: 進行中の対局は保存しない（毎回新規対局）。`localStorage` に保存するのは以下のみ：
  - `bgmEnabled` … タイトル BGM のオン／オフ（デフォルト false）
  - `muteAll` … 効果音含む一括ミュート（デフォルト false）
  - `tutorialSeen` … チュートリアルを1回でも完了 or スキップしたか（デフォルト false）
  - `hintsEnabled` … きけんマーク（§5.8.2）の表示有無（デフォルト true）
- いずれもクリアされても問題ない情報のみ。個人情報・対局履歴は保存しない。
- **パフォーマンス**: 60fps を維持。重い画像は使わない。

## 8. ファイル構成（推奨）

```
電車将棋ゲーム/
├── CLAUDE.md
├── SPEC.md            (このファイル)
├── README.md          (遊び方を保護者向けに)
├── index.html
├── css/
│   └── style.css
├── js/
│   ├── main.js        (エントリーポイント)
│   ├── game.js        (ゲーム状態・ルール)
│   ├── board.js       (盤の描画とタッチ処理)
│   ├── pieces.js      (駒の定義と動きルール)
│   ├── ui.js          (ターン表示・ボタン・演出・効果音合成)
│   └── tutorial.js    (§5.7 チュートリアル。サンドボックス再生)
├── assets/
│   ├── images/        (駒アイコン PNG。成り駒は -promoted 接尾辞)
│   └── sounds/        (mp3 / ogg)
└── tests/
    └── manual.html    (手動テスト用チェックリスト)
```

## 9. やらないこと（スコープ外）

明示的に**作らない**:
- オンライン対戦・通信機能
- AI（コンピュータ対戦）※将来の拡張余地として設計上は分離しておくが、今回の MVP では実装しない
- ログイン・アカウント
- 課金・広告
- 持ち駒（取った駒の再使用）
- 王手判定
- 棋譜の保存・再生
- 日本語以外の言語

## 10. 完成の定義（Definition of Done）

以下が全て満たされたら完成とする:
1. iPhone / Android / **iPad 11** 実機ブラウザで、タップだけで開始から勝利まで一連の流れが遊べる。各端末で盤が画面に対して大きく中央配置されている（§5.1.1）。
2. 4〜6歳の子どもが、**保護者の手助けなしで**「チュートリアル → 対局 → しんかんせんを取って勝つ」体験ができる（§5.7）。
3. 動けるマスが緑のまるで明示される。相手の駒をタップすると紫のまるで動ける範囲が見え、危ない自分の駒には「！」マークが自動表示される（§5.8）。
4. 駒の移動でガタゴト音が、勝利時にファンファーレが鳴る（§5.5.1 / §5.5.2）。ミュートボタンで一括 OFF にできる。
5. 負けても泣かない演出が入っている。
6. `index.html` をダブルクリックするだけでローカル再生できる（ビルド不要）。音声ファイルへの依存はなく、Web Audio API で全て合成。
7. README.md に保護者向けの遊び方説明が書かれている。
