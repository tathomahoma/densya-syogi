# CLAUDE.md — 電車しょうぎ プロジェクト規約

このファイルは、Claude Code がこのリポジトリで作業する際の常時ガイドラインです。
新しい会話を開始するたびに最初に読んでください。

詳細なゲーム仕様は `SPEC.md` を参照してください。**SPEC.md と本ファイルが矛盾した場合は SPEC.md を優先**します。

---

## 0. ひとこと要約

未就学児（4〜6歳）が iPhone / Android のブラウザで遊ぶ、電車テーマの簡略版「将棋」。
**シンプル・タップしやすい・読めなくても遊べる** が三大原則。

## 1. 技術スタック（決定事項）

- **HTML / CSS / Vanilla JavaScript（IIFE パターン）のみ**。
- フレームワーク（React, Vue, Svelte 等）は **使わない**。
- ビルドツール（Vite, webpack, npm 等）は **使わない**。`index.html` をダブルクリックで動くこと。
- 外部 CDN への依存も**原則禁止**（オフラインで動作することが要件）。例外的に許可する場合は `SPEC.md` の §7 に追記すること。
- TypeScript は使わない。代わりに JSDoc 型注釈で型情報を残す。
- パッケージマネージャ（npm/yarn/pnpm）は使わない。`package.json` は作らない。
- JS の構文は **ES2017 程度までのモダン構文を使ってよい**（const/let、アロー関数、テンプレートリテラル、for...of、スプレッド等）。ただし **ES Modules（import/export）だけは使わない**。`file://` プロトコルで `<script src>` 直読みするため。

> **なぜか**: 保護者が GitHub からダウンロードして、すぐに子どもに遊ばせられるようにしたい。`npm install` のステップが入る瞬間にこのプロジェクトの目的は失われる。

## 2. 対象ユーザーと UX 原則

実装判断で迷ったときは、必ず以下の優先順位で考える：

1. **4〜6 歳の子どもが自力で操作できるか**
2. **保護者が説明なしで状況を把握できるか**
3. **将棋として正しいか**

3 が 1・2 と衝突したら**迷わず 1・2 を優先**してよい。これは将棋アプリではなく、子ども向けゲームである。

具体的なルール：
- すべての文字は**ひらがな**で表示。漢字を画面に出さない（コード内コメントは漢字 OK）。
- ボタンとマスの最小サイズは **60×60 CSS px**、推奨は 72px 以上。
- 「ダメな操作」をしたときに**赤いバツ・ブザー音・エラーメッセージを出さない**。無音で無視する。
- 「正解」「失敗」のような評価語を使わない。代わりに「やったね」「もういちど」のような言葉を使う。
- 色だけで意味を伝えない（色覚多様性配慮）。形・枠線・アイコンを併用する。
- アニメーションは 0.3 秒以内に収める。長すぎると子どもが飽きる。

## 3. ディレクトリ構成

```
電車将棋ゲーム/
├── CLAUDE.md          (このファイル)
├── SPEC.md            (ゲーム仕様)
├── README.md          (保護者向けの遊び方)
├── index.html
├── css/style.css
├── js/
│   ├── main.js        — エントリポイント。DOMContentLoaded で起動。
│   ├── game.js        — ゲーム状態（純粋ロジック、DOM に触らない）
│   ├── board.js       — 盤の描画とタッチ／クリック処理
│   ├── pieces.js      — 駒の定義と「どこに動けるか」関数
│   ├── ui.js          — ターン表示・ボタン・効果音合成・勝利演出
│   └── tutorial.js    — §5.7 チュートリアル。サンドボックス状態で再生
└── assets/
    └── images/        — 駒アイコンはユーザー提供の PNG（成り駒は -promoted 接尾辞）。
```

### モジュール境界のルール
- すべての JS ファイルは IIFE パターンで `window.DenshaShogi` 名前空間に関数を登録する。`file://` プロトコルでの動作を保証するため ES Modules は使わない。
- `game.js` は **DOM に一切触らない**。テストしやすくするため、純粋関数とイミュータブルな状態管理に保つ。
- `pieces.js` の関数は `(pieces, piece) => Position[]` のような形で、副作用を持たない。
- 描画関連は `board.js` と `ui.js` に閉じ込める。
- 効果音は Web Audio API で合成する（音声ファイル不要）。`ui.js` に集約。SPEC.md §5.5 に列挙したイベントごとの音色仕様に従う。ただし BGM 用途に限り、ライセンスが明確（CC0 / パブリックドメイン）な音声ファイルの追加を許可する。出所を `assets/CREDITS.md` に記載すること。
- `tutorial.js` は `game.js` の状態に触れず、デモ用の読み取り専用スナップショットを使う。本対局を中断したり破壊したりしない。
- §5.8「あいてのうごき可視化」は専用モジュールを作らず、既存責務に分配する：
  - **計算**（相手駒の動ける範囲・きけんマーク対象の算出）は `pieces.js` の純粋関数として、自分・相手どちらの側からも呼べる形にする。すでにある `movablePositionsOf(board, piece)` をそのまま再利用できる設計を維持すること。
  - **描画**（紫のまる・「！」マーク）は `board.js` に、状態管理（ピーク中の駒 ID・hintsEnabled）は `ui.js` または `game.js` の UI 状態側に置く。
  - 自分の駒選択状態とピーク状態は**相互排他**。両方同時に true になるバグを生まないよう、状態遷移を `ui.js` 側で集約管理する。
- AudioContext の初期化はタイトル画面の「はじめる」タップで一度だけ行い、`ui.js` 内で単一インスタンスとして保持する（iOS Safari 対応）。

## 4. コーディング規約

- インデントは**スペース2つ**。
- セミコロンは**つける**。
- 変数宣言は **`const` を基本**とし、再代入が必要な場合のみ `let`。**`var` は使わない**（ループ内クロージャのバグ防止と再代入検出のため）。
- 関数は**アロー関数を基本**とする。名前空間への公開も `ns.foo = (args) => {...}` の形。
- 文字列はシングルクォート `'...'`、結合が入る場合はテンプレートリテラル（バッククォート）を優先。
- 変数名・関数名は **英語の lowerCamelCase**（例: `selectedPiece`, `movablePositions`）。
  - ただしユーザーに見える定数（駒の表示名など）は日本語の文字列リテラルでよい：`{ name: 'えきちょう' }`。
- ファイル先頭に簡潔な日本語コメントで「このファイルの責任」を 1〜3 行で書く。
- マジックナンバー禁止。盤サイズ（`ROWS`, `COLS`）・成りゾーン（`PROMOTION_ZONE`）などは `pieces.js` 冒頭の `ns.*` に集約する。
- console.log のデバッグ出力は**コミット前に消す**。
- グローバル変数は `window.DenshaShogi` 名前空間のみ許可。各ファイルは IIFE で閉じる。

### JSDoc の例
```js
/**
 * 指定した駒が動ける全マスを返す。
 * @param {Board} board
 * @param {Piece} piece
 * @returns {Position[]} 動けるマスのリスト（盤外・自駒のあるマスは除く）
 */
ns.movablePositionsOf = (board, piece) => { ... };
```

### 型の代わりの JSDoc typedef
```js
/**
 * @typedef {Object} Position
 * @property {number} row  - 1〜6
 * @property {number} col  - 1〜7
 *
 * @typedef {'red' | 'blue'} Side
 * @typedef {'futsuu' | 'tokkyuu' | 'kamotsu' | 'shinkansen'} PieceType
 *
 * @typedef {Object} Piece
 * @property {string} id
 * @property {Side} side
 * @property {PieceType} type
 * @property {Position} pos
 * @property {boolean} captured
 * @property {boolean} promoted
 */
```

## 5. テスト方針

- **テスト用フレームワークは入れない**（依存ゼロの方針）。代わりに `tests/manual.html` を用意し、簡単な手動チェックリストをブラウザで開いて確認する。
- ゲームロジック (`game.js`, `pieces.js`) は純粋関数の集合にして、`tests/manual.html` 内で `console.assert` ベースの最小検証ができるようにする。
- UI 系（`board.js`, `ui.js`）は実機タップで確認することを前提に、ロジックを薄くしておく。

## 6. やってはいけないこと

- `npm install` / `package.json` 追加 / ビルド工程の導入 — 依存ゼロ要件に違反します。
- React / Vue / TypeScript / Tailwind 等の導入 — 同上。
- 外部 API・トラッキング・広告コードの埋め込み。
- ユーザーデータの収集や送信。
- アラートのような大きな効果音、点滅、ジャンプスケア演出。ガタゴト音やファンファーレも「短く・優しく・合成」が原則（SPEC.md §5.5.4）。
- 効果音のために音声ファイル（mp3/ogg 等）を新規追加すること。効果音は Web Audio API での合成で賄う。ただし BGM 用途に限り、ライセンスが明確（CC0 / パブリックドメイン）な音声ファイルの追加を許可する。出所を `assets/CREDITS.md` に記載すること。
- 課金・購入を匂わせる UI。
- 文字を読めない子に不利になる仕様（漢字、長文の説明、文字だけのボタン）。
- 「将棋として正しい」を理由に未就学児が混乱するルール（持ち駒・王手判定など）を追加すること。

## 7. コミット・PR の作法

- コミットメッセージは日本語で簡潔に。例: `盤面の初期配置を実装` / `かくえきが斜めに動けるバグを修正`
- 1 コミット 1 トピックを心がける。
- PR を作る場合、SPEC.md の「完成の定義」のどれを満たすか明記する。

## 8. 困ったときの判断指針

- 「これ将棋的にどうすべき？」→ **そもそも将棋ルールに合わせない**。SPEC.md を見て、それでも書いてなければ「未就学児にとって直感的なほう」を選ぶ。
- 「ライブラリ入れたほうが楽そう」→ **入れない**。やりたいことを 50 行の素朴な JS で書く。
- 「もっと派手な演出がいい？」→ **静かなほうがいい**。子どもが疲れないことを優先。
- 「機能を増やしたい」→ SPEC.md の §9「やらないこと」に該当しないか確認する。該当すれば追加しない。
- 「チュートリアルにこの説明も入れたい」→ ステップ数を増やすより、既存ステップの見せ方を磨く方向で検討する。子どもの集中力は 90 秒が限度。
- 「音をもっと派手にしたい」→ SPEC.md §5.5.4「短く・優しく・合成」に違反しないか確認。違反する場合は私（ユーザー）に確認を取る。

## 9. 参考リンク（外部コンテンツの取り扱い）

- 将棋の公式ルールは参考程度に留め、本作のルール定義は **常に SPEC.md が正**。
- 既存の将棋アプリの挙動をそのまま真似しない（ターゲットが違う）。
- 効果音・画像素材を Web からコピーする場合は、必ずライセンス（CC0 / パブリックドメイン）を確認すること。出所を `assets/CREDITS.md` に記載する。
